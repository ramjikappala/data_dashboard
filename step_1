import requests
import pandas as pd
from datetime import datetime
from io import BytesIO

# 1. SET THE TRIAL DATE
target_date = "30042026" 

# 2. THE CORRECT URL FORMAT
# Note: Archives uses lowercase 'full' and the date format must match
url = f"https://nseindia.com_{target_date}.csv"

# 3. THE "SECRET" HEADERS (This prevents the block)
headers = {
    'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36',
    'Accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,*/*;q=0.8',
    'Referer': 'https://nseindia.com'
}

print(f"Downloading data for {target_date}...")
response = requests.get(url, headers=headers, timeout=30)

if response.status_code == 200:
    df = pd.read_csv(BytesIO(response.content))
    # ... rest of your code to build the HTML ...
    print("Success! Data found.")
else:
    print(f"Failed! Status Code: {response.status_code}")
    # If it fails with 403 or 404, it means the robot is still being blocked
